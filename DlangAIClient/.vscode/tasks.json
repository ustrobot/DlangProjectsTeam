{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "dub",
			"run": true,
			"cwd": "${workspaceFolder}",
			"compiler": "$current",
			"archType": "$current",
			"buildType": "$current",
			"configuration": "$current",
			"problemMatcher": [
				"$dmd"
			],
			"group": "build",
			"label": "dub: Run dlang-ai-client",
			"detail": "dub run --compiler=dmd -a=x86_64 -b=debug -c=application"
		}
	]
}